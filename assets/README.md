# Constructor Fabric assets

This folder now contains the material extracted from manifest.parts under assets/manifest-parts/.
The main manifest can reference files from the GitHub repo constructor-fabric using raw URLs or a baseUrl.
